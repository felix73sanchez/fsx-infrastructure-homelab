[DEFAULT]
bantime = {{ fail2ban_bantime | default(3600) }}
findtime = 600
maxretry = {{ fail2ban_maxretry | default(5) }}
backend = systemd

[sshd]
enabled = true
port = {{ ssh_port | default(22) }}
logpath = %(sshd_log)s
